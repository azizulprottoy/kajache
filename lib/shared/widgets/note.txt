// Button
CustomButton(
  label: 'Submit',
  variant: ButtonVariant.primary,
  isLoading: controller.isLoading,
  isFullWidth: true,
  prefixIcon: const Icon(Icons.check),
  onPressed: controller.submit,
)

// TextField
CustomTextField(
  label: 'Email',
  hint: 'Enter your email',
  controller: emailCtrl,
  keyboardType: TextInputType.emailAddress,
  prefixIcon: const Icon(Icons.email_outlined),
  validator: (v) => v!.isEmpty ? 'Required' : null,
)

// Password field (toggle built-in)
CustomTextField(
  label: 'Password',
  obscureText: true,
  controller: passCtrl,
  prefixIcon: const Icon(Icons.lock_outline),
)

// Error
AppErrorWidget.network(onAction: controller.retry)
AppErrorWidget.empty(title: 'No Jobs Found')

// Loading overlay
LoadingOverlay(
  isLoading: controller.isLoading,
  child: YourPageContent(),
)

// Rating (interactive)
RatingBarWidget(
  initialRating: 3.5,
  onRatingUpdate: (r) => controller.setRating(r),
)

// Rating (display only)
RatingBarWidget.display(initialRating: 4.0, itemSize: 18)